# This file contains patches that will be applied to the combined Kong configuration
# It's useful for overriding settings from the individual team configurations

_format_version: "3.0"

patches:
  # Example: Add a global rate limiting plugin to all services
  # - selectors:
  #   - $.services[*]
  #   values:
  #     plugins:
  #       - name: rate-limiting
  #         config:
  #           minute: 100
  #           policy: local
